# Cold read — 2026-08-19

- **Commit tested:** `dfdc0f8`
- **Answer key valid as of:** `dfdc0f8`
- **Run by:** Claude, at Brandon's request, end of the 2026-08-18/19 session

## Score

- Questions 1-9 matching the key: **9 / 9**
- Defects raised in Q10: **9** (8 confirmed, 0 wrong, 1 environment issue outside the docs)

The agent disclosed that it had printed the first 20 lines of `answer-key.md` before
realising what the file was, covering the answers to Q1 and Q2. Those two are contaminated
and should not be counted as independent. Q3 through Q9 are clean and all matched.

## Divergences in Q1-9

None. It reconstructed the repo layout, the publishing path, the `@/` citation mechanism
including the generated source pages, the scope of the evidence standard, all five modes
plus the two project-specific ones, and the working rules — including the newest ones
(list mode, no AI tells, fetch before push, `whos-editing.sh`). It also volunteered two
steps nobody asked for: fetch first in case of a web-editor commit, and check
`whos-editing.sh` before editing. That is the documentation working.

## Q10 defects

| # | What it flagged | Verified? | Disposition |
|---|---|---|---|
| 1 | `CLAUDE.md` Orientation still described the removed `monologues/` draft pipeline and said hand-editing the article is "silently overwritten. Edit the draft." Directly contradicts the "one rule, no exceptions" section fifteen lines later. | **Yes** | **Fixed.** The bullet now states the single source of truth and records what it used to say and why it was wrong. This is the most serious defect the test has found: it would send a new session to edit a file nothing reads, and it survived three readings of the file during the session. |
| 2 | The `CLAUDE.md` copy injected into the agent's system prompt is an older, shorter version than the file on disk — missing reading cost, `ship.sh`, `whos-editing.sh`, list mode, the cold read, the voice-notes rule, and still carrying the stale draft bullet. | **Cannot verify from inside** | **For Brandon.** Not a repo defect. If the harness caches `CLAUDE.md`, a session may be working from a rolled-back rulebook without knowing it. |
| 3 | `WORKFLOW.md` says to stop the dev server with `pkill -f "astro dev"` at line 42, then explains at line 159 that this does not work. | **Yes** | **Fixed** — line 42 now gives `npx astro dev stop` and points at the explanation. |
| 4 | `WORKFLOW.md` still described `npm run watch` as "a watcher on `monologues/`." | **Yes** | **Fixed** — now describes `watch-content.mjs` re-running the citation build on changes under `web/content/`. |
| 5 | Two answers to "how do I publish": `npm run publish && git push` under a heading saying "one rule, no exceptions," and `ship.sh` buried in a later section. | **Yes** | **Fixed** — `ship.sh` is now the headline method, with the manual steps kept beneath it. |
| 6 | `reference/how-this-investigation-uses-ai.md` and `web/content/about/how-this-investigation-uses-ai.md` are two files with the same name in two trees, and they **differ** (3,282 vs 3,604 bytes). | **Yes** | **Partly fixed.** `CLAUDE.md` now names the published copy as canonical and flags the other. **Reconciling or deleting the `reference/` copy is Brandon's call.** |
| 7 | `state_of_tennessee/tn_sos_filings/` exists and is cited but has no row in the `DATA_MAP.md` table. | **Yes** | **Fixed** — row added. |
| 8 | The harness assigns a scratchpad under `/tmp`, which `~/.claude/CLAUDE.md` §6 forbids on this machine and explicitly overrides. | **Yes** | **For Brandon.** Environment, not repo. Pointing the harness scratchpad at `~/.claude/scratch/` would remove the trap. |
| 9 | Unclear whether an `@c` note stripped by the build (rather than resolved by hand) still needs archiving to the voice notes. | **Yes, genuinely ambiguous** | **Fixed** — the rule now says it applies either way. |

## Fixed in this pass

Items 1, 3, 4, 5, 7, 9 fully. Item 6 partially.

## Left open

- **Item 6:** which AI-policy file is canonical, and whether the `reference/` copy is
  deleted or reconciled.
- **Item 2:** the stale injected `CLAUDE.md`, if the harness is caching it.
- **Item 8:** the `/tmp` scratchpad assignment.

## Notes for the next run

The test earned its keep on the first item alone. A defect that had been sitting in the
most-read file in the project, through a fourteen-hour session in which that file was read
three times, was found by an agent that had never seen it before. **Warm sessions cannot
see stale text; they have already been told the correct version by the conversation.**

Method note: the run prompt should tell the agent that `files/cold-read/` exists and is
off limits. This agent found the answer key by accident and disclosed it honestly, which is
the good outcome, but the prompt should prevent it rather than rely on disclosure.
