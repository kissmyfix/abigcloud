# The working directory and the public repo are the same tree. This file is the line
# between them: everything here stays local. Evidence and reference material publish.

# --- Private: Claude's working state, never published ---
# Brandon's pen (`monologues/`) and the developed article substance (`angles/`) both moved
# out of the project on 2026-08-19, to ~/Documents/monologues/. Nothing here needs to
# ignore them any more; they are not in the tree.
memory/
.remember/
.claude/settings.local.json

# --- Machine-generated / rebuildable ---
files/venv/
**/__pycache__/
*.pyc

# --- Large binaries: too big for git, published elsewhere if at all ---
podcasts/mp3s/

# --- OS noise ---
.DS_Store
Thumbs.db

# --- Site build output (the site's source is tracked; its build is not) ---
web/dist/
web/.astro/
web/node_modules/

# --- Pulled 2026-08-17, pending the AI-disclosure policy update ---
# Not secret; withheld deliberately. These are agent-operating files and
# pre-publication working material, not evidence. Revisit each one when the
# disclosure policy is rewritten and republish what belongs in the open.
CLAUDE.md
.claude/
files/ai-toolchain-changelog.md
files/cold-read/

# --- Scratch and editor droppings. These regrow; keep them out for good. ---
# Scratch belongs in ~/.claude/scratch/<project>/, outside the tree entirely.
*.bak
*.whoops
*.orig
*.old
*~
*.swp
*.astro.index

# --- Held back 2026-08-28: the 600 Small Street / Children Are People work ---
# Hunch-stage. Theories, adjacencies and characterisations of named private individuals,
# none of it yet load-bearing for a published claim. Publish piece by piece as each claim
# gets sourced and lands in an article, not as a directory. `memory/` is already ignored
# above, so the day's MEMORY/TIMELINE/PINBOARD work is covered by that line, not this one.
sumner_county/600_small_street/
state_of_tennessee/tn_property_assessments/sumner-assessment-600-small-st-*.pdf
state_of_tennessee/tn_property_assessments/sumner-assessment-carson-st.pdf

# Player profiles opened 2026-08-28 from board-roster cross-referencing. Each names a
# living private individual against a theory, not a published finding. Lamberth is
# explicitly parked. Review before any of these publish.
the_players/derrick_jackson.md
the_players/don_cunningham.md
the_players/kee_bryant_mccormick.md
the_players/william_lamberth.md

# Intake staging — unfiled, unvetted material and its bookkeeping
inbox/
files/.archive-hashes
files/.intake.log
files/MANIFEST.csv
